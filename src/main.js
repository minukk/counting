import { useEffect } from "react";
import axios from 'axios';
import './main.css';

const Main = () => {
  const url = '';

  useEffect(() => {
    // axios.post(url).then().catch(error => console.error(error));
    window.location.replace('https://vo.la/W3f0Kd');
  }, []);

  return (
  <main>
    <img src="images/logo_transparent.png" />
  </main>)
}

export default Main;